---
input_summary: 用一句话描述这个示例场景（例如：为新品牌做营销，目标平台 Instagram，语气轻松）

# ── 以下字段 v1.5 后 deprecated（Portal 自动测试填写） ──
# test_level: smoke        # smoke | qa | prod_ready
# runtime: claude          # 你实际测试的 AI 平台
# model: claude-opus-4-6   # 你实际测试的完整模型名称
---

<!--
  说明：
  1. 复制这份文件为 example_01.md（或 example_02.md、example_03.md……）
  2. 填入一组真实测试过的 input / output
  3. 一份文件对应一个场景；复杂的 skill 建议写 2–3 个不同场景
  4. 提交前请删除这个说明区块
-->

## Input

**User provides:**

（在此填入模拟的 User 输入，尽量贴近真实使用场景。
如果 skill 需要多轮对话，请按顺序列出每一轮 User 的消息。）

---

## Output

**Skill response:**

（在此填入这个 Skill 应该产出的完整 output。
这就是 User 会拿到的预期结果，也是审查时的标准。）

---

## Notes（选用）

（说明这个示例在测试什么场景重点，或 output 里有哪些已知的取舍。
例如：「测试语气设为幽默时的创意发散程度。」）
